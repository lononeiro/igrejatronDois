{
  "dependencies": {
    "@prisma/client": "^7.2.0",
    "bcryptjs": "^3.0.3",
    "jsonwebtoken": "^9.0.3"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.10",
    "prisma": "^7.2.0"
  },
  "scripts": {
    "postinstall": "prisma generate",
    "build": "prisma generate && next build"
  }
}
